# ─── API ─────────────────────────────────────────────
# Backend API base URL. In dev, point to your local Rizqun API.
# In production, set this at build time to your deployed backend URL.
VITE_API_BASE_URL=http://localhost:3000
