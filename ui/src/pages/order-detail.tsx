import { PlaceholderPage } from './_placeholder';

export function OrderDetailPage() {
  return (
    <PlaceholderPage
      title="Order Detail"
      description="Full order detail with items, vendor groups, status timeline, audit log, actions. Replaces this placeholder in Phase 4."
      phase="4"
    />
  );
}
