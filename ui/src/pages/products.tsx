import { PlaceholderPage } from './_placeholder';

export function ProductsPage() {
  return (
    <PlaceholderPage
      title="Products"
      description="Paginated product list with filters + smart search. Admin sees create/edit/toggle. Replaces this placeholder in Phase 2.3-2.5."
      phase="2.3"
    />
  );
}
