import { PlaceholderPage } from './_placeholder';

export function NewOrderPage() {
  return (
    <PlaceholderPage
      title="New Order"
      description="Cart sidebar + customer info form + finalize. Replaces this placeholder in Phase 3.4."
      phase="3.4"
    />
  );
}
