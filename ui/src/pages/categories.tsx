import { PlaceholderPage } from './_placeholder';

export function CategoriesPage() {
  return (
    <PlaceholderPage
      title="Categories"
      description="Category CRUD (super_admin only). Replaces this placeholder in Phase 2.1."
      phase="2.1"
    />
  );
}
