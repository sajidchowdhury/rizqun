import { PlaceholderPage } from './_placeholder';

export function LoginPage() {
  return (
    <PlaceholderPage
      title="Login"
      description="Operator + admin login form. Replaces this placeholder in Phase 1.3."
      phase="1.3"
    />
  );
}
