import { PlaceholderPage } from './_placeholder';

export function OrdersDonePage() {
  return (
    <PlaceholderPage
      title="Done Orders"
      description="Delivered orders with month filter + search + CSV export. Replaces this placeholder in Phase 5.1."
      phase="5.1"
    />
  );
}
