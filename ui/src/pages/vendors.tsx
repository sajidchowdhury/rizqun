import { PlaceholderPage } from './_placeholder';

export function VendorsPage() {
  return (
    <PlaceholderPage
      title="Vendors"
      description="Paginated vendor list with CRUD + active toggle. Admin only. Replaces this placeholder in Phase 2.2."
      phase="2.2"
    />
  );
}
