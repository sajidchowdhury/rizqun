import { PlaceholderPage } from './_placeholder';

export function DashboardPage() {
  return (
    <PlaceholderPage
      title="Dashboard"
      description="KPI cards + 4 charts (orders per day, avg time per day, step times, category breakdown). Replaces this placeholder in Phase 6."
      phase="6"
    />
  );
}
