import { PlaceholderPage } from './_placeholder';

export function RatingFormPage() {
  return (
    <PlaceholderPage
      title="Customer Rating"
      description="Public rating form (no auth). Customer rates overall/speed/behavior + comment. Replaces this placeholder in Phase 8.2."
      phase="8.2"
    />
  );
}
