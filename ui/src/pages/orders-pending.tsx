import { PlaceholderPage } from './_placeholder';

export function OrdersPendingPage() {
  return (
    <PlaceholderPage
      title="Pending Orders"
      description="Operator's active queue — auto-refresh, search by customer, color-coded by age. Replaces this placeholder in Phase 3.5."
      phase="3.5"
    />
  );
}
