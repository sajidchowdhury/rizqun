import { PlaceholderPage } from './_placeholder';

export function UsersPage() {
  return (
    <PlaceholderPage
      title="Users"
      description="User CRUD + category access picker (super_admin only). Replaces this placeholder in Phase 7."
      phase="7"
    />
  );
}
